type = process
command = $PREFIX/lib/dinit/ipfs
restart = on-failure
smooth-recovery = yes
depends-on: network.target
logfile = $PREFIX/var/log/dinit/ipfs.log

@include-opt $PREFIX/etc/dinit.d/config/ipfs.override
