type = process
command = $PREFIX/bin/tor
restart = on-failure
smooth-recovery = yes
depends-on: network.target
logfile = $PREFIX/var/log/dinit/tor.log

@include-opt $PREFIX/etc/dinit.d/config/tor.override
