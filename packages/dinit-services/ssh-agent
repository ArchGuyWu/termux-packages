type = process
command = $PREFIX/bin/ssh-agent -D -a $SSH_AUTH_SOCK
env-file = config/ssh-agent.env
restart = on-failure
smooth-recovery = yes
depends-on: network.target
depends-ms: svlogd@ssh-agent
log-type = pipe

@include-opt $PREFIX/etc/dinit.d/config/ssh-agent.override
