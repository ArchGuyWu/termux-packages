type = process
command = $PREFIX/bin/jackett --DataFolder ${HOME}/.config/jackett
restart = on-failure
smooth-recovery = yes
depends-on: network.target
log-type = buffer

@include-opt $PREFIX/etc/dinit.d/config/jackett.override
