type = process
command = $PREFIX/lib/dinit/nginx
restart = on-failure
smooth-recovery = yes
depends-on: network.target
depends-ms: svlogd@nginx
log-type = pipe

@include-opt $PREFIX/etc/dinit.d/config/nginx.override
