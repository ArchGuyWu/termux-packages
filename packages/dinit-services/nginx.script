#!/data/data/com.termux/files/usr/bin/sh
mkdir -p $TERMUX_ANDROID_HOME/.nginx
if [ -f "$TERMUX_ANDROID_HOME/.nginx/nginx.conf" ]
then
  CONFIG="$TERMUX_ANDROID_HOME/.nginx/nginx.conf"
else
  CONFIG="$TERMUX_PREFIX/etc/nginx/nginx.conf"

exec nginx -p ~/.nginx -g "daemon off;" -c \$CONFIG
