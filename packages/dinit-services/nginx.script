#!/data/data/com.termux/files/usr/bin/sh
mkdir -p $HOME/.nginx
if [ -f "$HOME/.nginx/nginx.conf" ]
then
  CONFIG="$HOME/.nginx/nginx.conf"
else
  CONFIG="$PREFIX/etc/nginx/nginx.conf"

exec nginx -p ~/.nginx -g "daemon off;" -c \$CONFIG
