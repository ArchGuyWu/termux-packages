type = process
command = $PREFIX/bin/sshd -D -e
restart = on-failure
smooth-recovery = yes
depends-on: network.target
logfile = $PREFIX/var/log/dinit/sshd.log

@include-opt $PREFIX/etc/dinit.d/config/sshd.override
