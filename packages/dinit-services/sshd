type = process
command = $PREFIX/bin/sshd -D -e $EXTRA_ARGS
env-file = config/sshd.env
restart = on-failure
smooth-recovery = yes
depends-on: network.target
log-type = buffer

@include-opt $PREFIX/etc/dinit.d/config/sshd.override
